"use client"

import { useMemo, useState } from "react"
import { Activity, ArrowUpRight, Bell, CheckCircle2, ChevronRight, CircleDollarSign, Clock3, CreditCard, Crown, Download, FileText, LayoutDashboard, Menu, MoreHorizontal, Search, Settings2, ShieldCheck, Sparkles, TrendingUp, UserPlus, Users, XCircle } from "lucide-react"
import { Button } from "@/components/ui/button"
import { cn } from "@/lib/utils"

type Tab = "Overview" | "Users" | "Payments" | "Subscriptions" | "Plans" | "Notifications" | "Settings"

const nav: { label: Tab; icon: typeof LayoutDashboard }[] = [
  { label: "Overview", icon: LayoutDashboard }, { label: "Users", icon: Users }, { label: "Payments", icon: CreditCard }, { label: "Subscriptions", icon: Crown }, { label: "Plans", icon: FileText }, { label: "Notifications", icon: Bell }, { label: "Settings", icon: Settings2 },
]

const users = [
  { name: "Raihan Kabir", email: "raihan.kabir@gmail.com", id: "MVBD-10482", plan: "Premium", status: "Active", joined: "Sep 08, 2026", initials: "RK" },
  { name: "Nusrat Jahan", email: "nusrat.jahan@outlook.com", id: "MVBD-10481", plan: "2 Months", status: "Active", joined: "Sep 08, 2026", initials: "NJ" },
  { name: "Tanvir Ahmed", email: "tanvir.ahmed@gmail.com", id: "MVBD-10479", plan: "Trial", status: "Expiring soon", joined: "Sep 07, 2026", initials: "TA" },
  { name: "Sadia Sultana", email: "sadia.sultana@gmail.com", id: "MVBD-10476", plan: "Free", status: "No access", joined: "Sep 06, 2026", initials: "SS" },
]

const payments = [
  { user: "Fahim Hossain", id: "MVBD-10473", plan: "3 Months", amount: "৳50", trx: "TXN8K2M91P", time: "12 min ago" },
  { user: "Mim Akter", id: "MVBD-10470", plan: "2 Months", amount: "৳35", trx: "TXN5D7L42Q", time: "38 min ago" },
  { user: "Sakib Hasan", id: "MVBD-10468", plan: "Monthly", amount: "৳20", trx: "TXN1A9C83R", time: "1 hr ago" },
]

const stats = [
  { label: "Total users", value: "10,482", change: "+12.8%", icon: Users, tone: "cyan" },
  { label: "Active trials", value: "1,284", change: "+8.4%", icon: Clock3, tone: "amber" },
  { label: "Premium users", value: "6,847", change: "+18.2%", icon: Crown, tone: "violet" },
  { label: "Monthly revenue", value: "৳2.48L", change: "+22.5%", icon: CircleDollarSign, tone: "emerald" },
]

export default function HomePage() {
  const [activeTab, setActiveTab] = useState<Tab>("Overview")
  const [sidebarOpen, setSidebarOpen] = useState(false)
  const [query, setQuery] = useState("")
  const [notice, setNotice] = useState<string | null>(null)
  const filteredUsers = useMemo(() => users.filter((user) => `${user.name} ${user.email} ${user.id}`.toLowerCase().includes(query.toLowerCase())), [query])

  function showNotice(message: string) { setNotice(message); window.setTimeout(() => setNotice(null), 2400) }

  return (
    <main className="min-h-screen bg-background text-foreground">
      {notice && <div className="fixed right-4 top-4 z-50 flex items-center gap-2 rounded-xl border border-emerald-400/30 bg-emerald-400/10 px-4 py-3 text-sm text-emerald-200 shadow-2xl"><CheckCircle2 className="size-4" />{notice}</div>}
      <header className="sticky top-0 z-40 flex h-16 items-center justify-between border-b border-white/8 bg-background/90 px-4 backdrop-blur-xl md:px-8">
        <div className="flex items-center gap-3"><Button variant="ghost" size="icon" className="md:hidden" onClick={() => setSidebarOpen(!sidebarOpen)} aria-label="Toggle menu"><Menu /></Button><div className="flex size-9 items-center justify-center rounded-xl bg-primary text-primary-foreground font-black">M</div><div><div className="text-sm font-semibold tracking-tight">MVBD Admin</div><div className="hidden text-[11px] text-muted-foreground sm:block">MoviesVerseBD control center</div></div></div>
        <div className="flex items-center gap-3"><div className="hidden items-center gap-2 rounded-full border border-emerald-400/20 bg-emerald-400/8 px-3 py-1.5 text-xs text-emerald-300 sm:flex"><span className="size-1.5 rounded-full bg-emerald-400 shadow-[0_0_10px] shadow-emerald-400" />All systems operational</div><Button variant="ghost" size="icon" aria-label="Notifications" onClick={() => setActiveTab("Notifications")}><Bell /></Button><div className="flex size-8 items-center justify-center rounded-full bg-secondary text-xs font-semibold">AR</div></div>
      </header>
      <div className="mx-auto flex max-w-[1600px]">
        <aside className={cn("fixed inset-y-16 left-0 z-30 w-64 border-r border-white/8 bg-background px-4 py-6 transition-transform md:sticky md:top-16 md:block md:h-[calc(100vh-4rem)] md:translate-x-0", sidebarOpen ? "translate-x-0" : "-translate-x-full")}>
          <div className="mb-7 px-3 text-[10px] font-bold uppercase tracking-[0.18em] text-muted-foreground">Workspace</div><nav className="flex flex-col gap-1">{nav.map(({ label, icon: Icon }) => <button key={label} onClick={() => { setActiveTab(label); setSidebarOpen(false) }} className={cn("flex items-center gap-3 rounded-xl px-3 py-2.5 text-left text-sm transition", activeTab === label ? "bg-primary/12 text-primary" : "text-muted-foreground hover:bg-white/5 hover:text-foreground")}><Icon className="size-4" />{label}{label === "Payments" && <span className="ml-auto rounded-full bg-amber-400/15 px-2 py-0.5 text-[10px] text-amber-300">23</span>}</button>)}</nav>
          <div className="mt-auto hidden rounded-2xl border border-white/8 bg-card p-4 md:block"><div className="mb-3 flex items-center gap-2 text-xs font-medium"><ShieldCheck className="size-4 text-emerald-400" />Secure workspace</div><p className="text-[11px] leading-relaxed text-muted-foreground">Admin access is protected with server-side verification.</p></div>
        </aside>
        <section className="min-w-0 flex-1 px-4 py-7 md:px-8 md:py-9">
          <div className="mb-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="mb-2 flex items-center gap-2 text-xs font-medium text-primary"><Sparkles className="size-3.5" />Tuesday, September 08, 2026</p><h1 className="text-2xl font-semibold tracking-tight md:text-3xl">Good morning, Admin</h1><p className="mt-1 text-sm text-muted-foreground">Here&apos;s what&apos;s happening across MVBD today.</p></div><Button variant="outline" onClick={() => showNotice("Report export started")}><Download data-icon="inline-start" />Export report</Button></div>
          {activeTab === "Overview" && <Overview showNotice={showNotice} />}
          {activeTab === "Users" && <UsersView users={filteredUsers} query={query} setQuery={setQuery} />}
          {activeTab === "Payments" && <PaymentsView showNotice={showNotice} />}
          {activeTab === "Subscriptions" && <SubscriptionsView />}
          {activeTab === "Plans" && <PlansView />}
          {activeTab === "Notifications" && <NotificationsView showNotice={showNotice} />}
          {activeTab === "Settings" && <SettingsView />}
        </section>
      </div>
    </main>
  )
}

function Overview({ showNotice }: { showNotice: (message: string) => void }) { return <div className="flex flex-col gap-6"><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{stats.map(({ label, value, change, icon: Icon, tone }) => <div key={label} className="rounded-2xl border border-white/8 bg-card p-5"><div className="mb-5 flex items-center justify-between"><span className="text-xs text-muted-foreground">{label}</span><div className={cn("flex size-8 items-center justify-center rounded-lg", tone === "cyan" && "bg-cyan-400/10 text-cyan-300", tone === "amber" && "bg-amber-400/10 text-amber-300", tone === "violet" && "bg-violet-400/10 text-violet-300", tone === "emerald" && "bg-emerald-400/10 text-emerald-300")}><Icon className="size-4" /></div></div><div className="flex items-end justify-between"><span className="text-2xl font-semibold tracking-tight">{value}</span><span className="flex items-center gap-1 text-xs text-emerald-300"><TrendingUp className="size-3" />{change}</span></div></div>)}</div><div className="grid gap-6 xl:grid-cols-[1.4fr_1fr]"><div className="rounded-2xl border border-white/8 bg-card p-5"><div className="mb-6 flex items-center justify-between"><div><h2 className="font-semibold">Revenue overview</h2><p className="mt-1 text-xs text-muted-foreground">Subscription revenue over the last 30 days</p></div><Button variant="ghost" size="sm">Last 30 days <ChevronRight data-icon="inline-end" /></Button></div><div className="flex h-52 items-end gap-2 border-b border-white/8 pb-0">{[42,48,38,58,52,67,62,78,70,76,86,80,91,84,96,88,100,92,108,98,112,105,118,110,126,120,134,128,144,150].map((height, index) => <div key={index} className="group relative flex flex-1 items-end"><div style={{ height: `${height / 1.6}%` }} className="w-full rounded-t-md bg-primary/65 transition group-hover:bg-primary" /></div>)}</div><div className="mt-4 flex justify-between text-[11px] text-muted-foreground"><span>Aug 10</span><span>Aug 20</span><span>Aug 30</span><span>Sep 08</span></div></div><div className="rounded-2xl border border-white/8 bg-card p-5"><div className="mb-5 flex items-center justify-between"><div><h2 className="font-semibold">Pending payments</h2><p className="mt-1 text-xs text-muted-foreground">Needs your review</p></div><span className="rounded-full bg-amber-400/12 px-2.5 py-1 text-xs text-amber-300">23 pending</span></div><div className="flex flex-col gap-4">{payments.map((payment) => <div key={payment.trx} className="flex items-center gap-3"><div className="flex size-9 items-center justify-center rounded-full bg-secondary text-xs font-semibold">{payment.user.split(" ").map((x) => x[0]).join("")}</div><div className="min-w-0 flex-1"><div className="truncate text-sm font-medium">{payment.user}</div><div className="text-xs text-muted-foreground">{payment.plan} · {payment.time}</div></div><div className="text-right"><div className="text-sm font-medium">{payment.amount}</div><div className="text-[10px] text-muted-foreground">{payment.trx}</div></div></div>)}</div><Button className="mt-5 w-full" variant="outline" onClick={() => showNotice("Payments tab opened")}>Review all payments <ArrowUpRight data-icon="inline-end" /></Button></div></div><div className="rounded-2xl border border-white/8 bg-card p-5"><div className="mb-5 flex items-center justify-between"><div><h2 className="font-semibold">Recent activity</h2><p className="mt-1 text-xs text-muted-foreground">Latest events from your platform</p></div><Activity className="size-4 text-muted-foreground" /></div><div className="flex flex-col gap-5">{[{ icon: UserPlus, text: "New user registered", detail: "Raihan Kabir joined MVBD", time: "4 min ago", tone: "text-cyan-300 bg-cyan-400/10" }, { icon: CircleDollarSign, text: "Payment received", detail: "৳50 from Fahim Hossain", time: "12 min ago", tone: "text-emerald-300 bg-emerald-400/10" }, { icon: Crown, text: "Plan upgraded", detail: "Nusrat Jahan upgraded to 2 Months", time: "38 min ago", tone: "text-violet-300 bg-violet-400/10" }, { icon: Bell, text: "Broadcast sent", detail: "Weekend maintenance notice", time: "1 hr ago", tone: "text-amber-300 bg-amber-400/10" }].map(({ icon: Icon, text, detail, time, tone }) => <div key={text} className="flex items-start gap-3"><div className={cn("flex size-8 shrink-0 items-center justify-center rounded-lg", tone)}><Icon className="size-4" /></div><div className="min-w-0 flex-1"><div className="text-sm font-medium">{text}</div><div className="truncate text-xs text-muted-foreground">{detail}</div></div><span className="whitespace-nowrap text-[11px] text-muted-foreground">{time}</span></div>)}</div></div></div> }

function UsersView({ users, query, setQuery }: { users: typeof users; query: string; setQuery: (value: string) => void }) { return <div className="rounded-2xl border border-white/8 bg-card"><div className="flex flex-col gap-4 border-b border-white/8 p-5 md:flex-row md:items-center md:justify-between"><div><h2 className="font-semibold">Users</h2><p className="mt-1 text-xs text-muted-foreground">Manage and monitor your member base</p></div><div className="relative w-full md:w-72"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search users..." className="h-9 w-full rounded-lg border border-white/10 bg-secondary/50 pl-9 pr-3 text-sm outline-none placeholder:text-muted-foreground focus:border-primary/60" /></div></div><div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead className="border-b border-white/8 text-xs text-muted-foreground"><tr><th className="px-5 py-3 font-medium">User</th><th className="px-5 py-3 font-medium">MVBD ID</th><th className="px-5 py-3 font-medium">Plan</th><th className="px-5 py-3 font-medium">Status</th><th className="px-5 py-3 font-medium">Joined</th><th className="px-5 py-3" /></tr></thead><tbody>{users.map((user) => <tr key={user.id} className="border-b border-white/6 last:border-0 hover:bg-white/[0.02]"><td className="px-5 py-4"><div className="flex items-center gap-3"><div className="flex size-8 items-center justify-center rounded-full bg-secondary text-[11px] font-semibold">{user.initials}</div><div><div className="font-medium">{user.name}</div><div className="text-xs text-muted-foreground">{user.email}</div></div></div></td><td className="px-5 py-4 font-mono text-xs text-primary">{user.id}</td><td className="px-5 py-4">{user.plan}</td><td className="px-5 py-4"><span className={cn("rounded-full px-2.5 py-1 text-xs", user.status === "Active" ? "bg-emerald-400/10 text-emerald-300" : user.status === "Expiring soon" ? "bg-amber-400/10 text-amber-300" : "bg-secondary text-muted-foreground")}>{user.status}</span></td><td className="px-5 py-4 text-muted-foreground">{user.joined}</td><td className="px-5 py-4 text-right"><Button variant="ghost" size="icon-sm" aria-label={`More actions for ${user.name}`}><MoreHorizontal /></Button></td></tr>)}</tbody></table></div></div> }

function PaymentsView({ showNotice }: { showNotice: (message: string) => void }) { return <div className="rounded-2xl border border-white/8 bg-card p-5"><div className="mb-6 flex items-center justify-between"><div><h2 className="font-semibold">Payment requests</h2><p className="mt-1 text-xs text-muted-foreground">Review manual subscription payments</p></div><span className="rounded-full bg-amber-400/12 px-3 py-1 text-xs text-amber-300">23 pending</span></div><div className="flex flex-col gap-3">{payments.map((payment) => <div key={payment.trx} className="flex flex-col gap-4 rounded-xl border border-white/8 bg-secondary/20 p-4 md:flex-row md:items-center"><div className="flex size-10 items-center justify-center rounded-full bg-amber-400/10 text-amber-300"><CreditCard className="size-4" /></div><div className="min-w-0 flex-1"><div className="font-medium">{payment.user}</div><div className="text-xs text-muted-foreground">{payment.id} · {payment.trx}</div></div><div className="text-sm">{payment.plan}</div><div className="font-semibold">{payment.amount}</div><div className="flex gap-2"><Button size="sm" onClick={() => showNotice(`${payment.user}'s payment approved`)}>Approve</Button><Button size="sm" variant="destructive" onClick={() => showNotice(`${payment.user}'s payment rejected`)}>Reject</Button></div></div>)}</div></div> }

function SubscriptionsView() { return <div className="grid gap-4 md:grid-cols-3">{[{ label: "Active subscriptions", value: "6,204", icon: CheckCircle2, tone: "text-emerald-300 bg-emerald-400/10" }, { label: "Expiring this week", value: "184", icon: Clock3, tone: "text-amber-300 bg-amber-400/10" }, { label: "Expired subscriptions", value: "937", icon: XCircle, tone: "text-rose-300 bg-rose-400/10" }].map(({ label, value, icon: Icon, tone }) => <div key={label} className="rounded-2xl border border-white/8 bg-card p-5"><div className={cn("mb-4 flex size-9 items-center justify-center rounded-xl", tone)}><Icon className="size-4" /></div><div className="text-2xl font-semibold">{value}</div><div className="mt-1 text-sm text-muted-foreground">{label}</div></div>)}</div> }

function PlansView() { return <div className="grid gap-4 md:grid-cols-3">{[{ name: "Monthly", price: "৳20", duration: "30 days", save: "Save 10%" }, { name: "Two Months", price: "৳35", duration: "60 days", save: "Save 20%", popular: true }, { name: "Three Months", price: "৳50", duration: "90 days", save: "Save 30%" }].map((plan) => <div key={plan.name} className={cn("relative rounded-2xl border bg-card p-6", plan.popular ? "border-primary/50" : "border-white/8")}>{plan.popular && <span className="absolute right-4 top-4 rounded-full bg-primary/15 px-2.5 py-1 text-[10px] font-bold uppercase tracking-wider text-primary">Popular</span>}<Crown className="mb-5 size-5 text-primary" /><h3 className="font-semibold">{plan.name}</h3><div className="mt-3 text-3xl font-semibold">{plan.price}<span className="ml-1 text-sm font-normal text-muted-foreground">/ {plan.duration}</span></div><p className="mt-2 text-xs text-emerald-300">{plan.save}</p><div className="mt-6 flex flex-col gap-3 border-t border-white/8 pt-5 text-sm text-muted-foreground"><div className="flex items-center gap-2"><CheckCircle2 className="size-4 text-primary" />Premium access</div><div className="flex items-center gap-2"><CheckCircle2 className="size-4 text-primary" />Telegram access included</div><div className="flex items-center gap-2"><CheckCircle2 className="size-4 text-primary" />Fast premium experience</div></div></div>)}</div> }

function NotificationsView({ showNotice }: { showNotice: (message: string) => void }) { const [message, setMessage] = useState(""); return <div className="max-w-2xl rounded-2xl border border-white/8 bg-card p-5"><h2 className="font-semibold">Send notification</h2><p className="mt-1 text-xs text-muted-foreground">Deliver an in-app message to a specific user.</p><label className="mt-6 flex flex-col gap-2 text-sm"><span className="text-muted-foreground">User UID or MVBD ID</span><input className="h-10 rounded-lg border border-white/10 bg-secondary/40 px-3 outline-none focus:border-primary/60" placeholder="MVBD-10482" /></label><label className="mt-4 flex flex-col gap-2 text-sm"><span className="text-muted-foreground">Message</span><textarea value={message} onChange={(event) => setMessage(event.target.value)} className="min-h-28 resize-none rounded-lg border border-white/10 bg-secondary/40 p-3 outline-none focus:border-primary/60" placeholder="Enter notification message..." /></label><Button className="mt-5" disabled={!message.trim()} onClick={() => { showNotice("Notification sent successfully"); setMessage("") }}><Bell data-icon="inline-start" />Send notification</Button></div> }

function SettingsView() { return <div className="max-w-2xl rounded-2xl border border-white/8 bg-card p-5"><h2 className="font-semibold">Admin settings</h2><p className="mt-1 text-xs text-muted-foreground">Workspace status and security configuration.</p><div className="mt-6 flex flex-col gap-3">{[{ label: "Environment", value: "Production" }, { label: "Admin verification", value: "Server-side" }, { label: "Database", value: "Firestore" }, { label: "Custom claims", value: "admin: true" }].map((item) => <div key={item.label} className="flex items-center justify-between rounded-xl border border-white/8 bg-secondary/20 px-4 py-3"><span className="text-sm text-muted-foreground">{item.label}</span><span className="text-sm font-medium">{item.value}</span></div>)}</div><div className="mt-5 flex items-start gap-3 rounded-xl border border-emerald-400/20 bg-emerald-400/8 p-4"><ShieldCheck className="mt-0.5 size-4 shrink-0 text-emerald-300" /><div><div className="text-sm font-medium text-emerald-200">All systems ready</div><div className="mt-1 text-xs text-emerald-200/70">Your admin dashboard is configured for secure server-verified operations.</div></div></div></div> }
